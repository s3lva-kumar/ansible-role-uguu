---
dependency:
  name: galaxy
driver:
  name: vagrant
provider:
    name: virtualbox
lint:
  name: yamllint
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: default
  test_sequence:
    - lint
    - create
    - converge
    - destroy
verifier:
  name: ansible